output_dir: /pfs/training-out

# Parallelism
ulysses_sequence_parallel_size: 1

# Memory management
enable_activation_checkpointing: True  # True reduces memory
enable_activation_offloading: False  # True reduces memory

# Tokenizer
tokenizer:
  _component_: mirotrain.models.qwen3.qwen3_tokenizer_auto
  path: /workspace/math_science_data/lyc/models/DR-Tulu-8B
  max_seq_len: 32768

# Dataset
dataset:
  _component_: torchtune.datasets.preference_dataset
  source: json
  data_files: /workspace/math_science_data/lyc/1205/MiroTrain/dpo_dataset.jsonl
  train_on_input: False
  split: train
seed: 1024
shuffle: True

# Model Arguments
model:
  _component_: mirotrain.models.qwen3.qwen3_8b_instruct

checkpointer:
  _component_: torchtune.training.FullModelHFCheckpointer
  checkpoint_dir: /workspace/math_science_data/lyc/models/DR-Tulu-8B
  checkpoint_files: [
    model-00001-of-00004.safetensors,
    model-00002-of-00004.safetensors,
    model-00003-of-00004.safetensors,
    model-00004-of-00004.safetensors,
  ]
  recipe_checkpoint: null
  output_dir: ${output_dir}
  model_type: QWEN3
resume_from_checkpoint: False


# The ref_checkpointer should always point to the original weights.
ref_checkpointer:
  _component_: torchtune.training.FullModelHFCheckpointer
  checkpoint_dir: /workspace/math_science_data/lyc/models/DR-Tulu-8B
  checkpoint_files: [
    model-00001-of-00004.safetensors,
    model-00002-of-00004.safetensors,
    model-00003-of-00004.safetensors,
    model-00004-of-00004.safetensors,
  ]
  recipe_checkpoint: null
  output_dir: ${output_dir}
  model_type: QWEN3


## Fine-tuning arguments
batch_size: 1
epochs: 3
gradient_accumulation_steps: 1  # Use to increase effective batch size

# loss:
loss:
  _component_: mirotrain.modules.loss.DPOLoss
  beta: 0.1
  loss_config: {"sigmoid": 1.0}
  # loss_config: {"sigmoid": 1.0, "sft": 0.1}

optimizer:
  _component_: torch.optim.AdamW
  fused: True
  weight_decay: 0.05
  lr: 1e-6

lr_scheduler:
  _component_: mirotrain.training.get_cosine_schedule_with_warmup
  warmup_ratio: 0.1
  num_cycles: 0.5
# lr_scheduler:
#   _component_: torchtune.training.lr_schedulers.get_cosine_schedule_with_warmup
#   num_warmup_steps: 20

max_steps_per_epoch: null
clip_grad_norm: null
compile: False  # torch.compile the model + loss, True increases speed + decreases memory
optimizer_in_bwd: False  # True saves memory. Requires gradient_accumulation_steps=1

# Training env
device: cuda

# Reduced precision
dtype: bf16

# Logging
metric_logger:
  _component_: torchtune.training.metric_logging.DiskLogger
  log_dir: ${output_dir}/logs
log_every_n_steps: 1
log_peak_memory_stats: False
log_level: INFO  # DEBUG, WARN, etc.

# Profiler (disabled)
profiler:
  _component_: torchtune.training.setup_torch_profiler
  enabled: False

  #Output directory of trace artifacts
  output_dir: ${output_dir}/profiling_outputs

  #`torch.profiler.ProfilerActivity` types to trace
  cpu: True
  cuda: True

  #trace options passed to `torch.profiler.profile`
  profile_memory: False
  with_stack: False
  record_shapes: True
  with_flops: False

  # `torch.profiler.schedule` options:
  # wait_steps -> wait, warmup_steps -> warmup, active_steps -> active, num_cycles -> repeat
  wait_steps: 5
  warmup_steps: 3
  active_steps: 2
  num_cycles: 1
